# CLAUDE.md

수중드론 수질측정 대시보드(센서 프로브–ESP32–라즈베리파이–웹 대시보드) 프로젝트의 Claude 행동 지침입니다. 하드웨어 센서 연동, 실시간 통신, 웹 시각화에서의 실수를 방지하기 위해 반드시 준수하세요.

**트레이드오프: 속도보다 하드웨어 안정성·데이터 정확성 우선.**

## 0. 시스템 구성 (고정 — 임의 변경 금지)

```
센서 프로브 (수중 0.5 / 1.0 / 1.5 m, 보트에서 유선 케이블로 직접 하강)
  └─ EC/TDS 일체형 센서 RS485(USB 어댑터) → 라즈베리파이 (조사 보트, FastAPI + SQLite, 핫스팟 AP)
       ├─ WebSocket(실시간) + REST(누적 조회·내보내기) → 대시보드 (보트 위, 핫스팟)
       └─ forwarder → LTE → NCP 클라우드 서버 (미러) → 대시보드 (원격, 아무 인터넷)

윈치: 별도 제어(대시보드가 구동하지 않음). 대시보드 명령(내림/정지/올림/auto)은
      수심 추정 상태기계 입력으로만 쓰인다 — 실제 윈치 조작과 병행 입력 필요.
```

### 하이브리드 저장 구조 (사용자 확정, 2026-08-11)

원격 모니터링을 위해 네이버 클라우드(NCP) 서버를 추가하되, **저장의 정본은 라즈베리파이다.**

- **라즈베리파이 = 정본(source of truth).** 지금의 `server/`를 그대로 유지 — feed.py 수신, 수심 추정, SQLite 저장, 보트 위 대시보드 브로드캐스트. **클라우드가 죽어도 측정은 정상 진행된다.**
- **클라우드 = 미러(mirror).** 라즈베리파이가 보낸 것을 받아 저장·중계할 뿐, 스스로 수심 추정이나 상태기계를 돌리지 않는다.
- **LTE 끊김은 정상 시나리오다.** 끊긴 동안 측정 레코드는 라즈베리파이 SQLite에 계속 쌓이고, 재접속 시 빠진 구간을 자동 전송(백필)한다. 6절 "1회 조사 전체 기록 무손실"은 라즈베리파이 SQLite가 보장한다.
- **전체 이전을 채택하지 않은 이유**: 저수지에서 LTE가 끊기면 그 구간 측정이 영구 손실되어 6절 기준을 만족할 수 없다.

### forwarder 구현 (사용자 확정, 2026-09-13)

`raspi/forward.py` — 라즈베리파이 → 클라우드 중계기. 별도 프로세스(`uwd-forward.service`)다.

- **Pi 의 `/ws/dashboard` 를 대시보드처럼 구독**한다. live·측정 레코드가 이미 그 채널로
  나오므로 engine 을 고칠 필요가 없다. 이 연결은 **단방향** — 명령은 절대 보내지 않는다.
- 받은 것을 클라우드 `/ws/mirror` 로 넘긴다. live 는 중계만(저장 안 함), 레코드는 저장+중계.
- **백필**: 접속할 때마다 미러가 `{"type":"ready","max_ts":N}` 로 자기 최신 ts 를 알려 주고,
  forwarder 가 Pi 의 REST(`/surveys`·`/records`)에서 그보다 새 레코드만 읽어 보낸다.
  같은 `(survey, ts, depth)` 는 같은 측정이므로 미러가 중복을 무시한다.
- **live 는 백필하지 않는다.** live 는 저장 대상이 아니라 되돌릴 원본이 없다 —
  끊긴 구간은 원격 화면에서 공백으로 남는 것이 맞다(6절).
- 차수 행은 레코드보다 **먼저** 보낸다. 미러는 Pi 의 `surveys.id` 를 그대로 복제한다 —
  id 를 새로 매기면 레코드의 survey 참조가 어긋난다.

미러 쪽(`UWD_MIRROR=1`): `engine.run()` 을 돌리지 않는다. 상태기계도 목업도 없고,
받은 값을 그대로 저장·중계만 한다. `/ws/mirror` 는 미러가 아닌 서버에서는 **열리지 않는다** —
보트 위 정본에 이 구멍이 있으면 외부에서 측정 기록을 주입할 수 있다.

인증은 **공유 토큰** `UWD_MIRROR_TOKEN` 이다(사용자 확정 2026-09-13). 양쪽 모두
`/etc/uwd-mirror.env`(600) 로만 주입한다 — 유닛 파일에 쓰면 `systemctl cat` 으로 읽히고,
명령줄에 쓰면 `ps` 에 보인다. 토큰이 비면 미러는 **기동을 거부한다.**

경계 (넘지 말 것):
- ~~클라우드 서버는 윈치 명령을 수신하지 않는다. 원격 대시보드는 읽기 전용.~~
  **철회 (사용자 확정, 2026-09-13).** 클라우드 대시보드가 **조작자의 메인 화면**이 됐다 —
  라즈베리파이가 핫스팟으로 인터넷을 받고, 팀이 보트 위에서 클라우드 화면으로 보고 조작한다.
  원래 금지 사유는 "원격의 제3자 ≠ 보트 조작자 → 수심 어긋남" 이었는데, 누르는 사람이
  곧 윈치를 만지는 사람이므로 그 전제가 성립하지 않는다.

  **대신 지켜야 하는 것 (이게 원칙의 알맹이다):**
  - **상태기계는 여전히 라즈베리파이에만 있다.** 클라우드는 버튼 누름을 forwarder 로
    넘기기만 하고 수심을 적분하지 않는다. 클라우드가 자체 적분하면 보트와 클라우드가
    서로 다른 수심을 말하게 되고, 둘 중 뭐가 맞는지 사후에 알 방법이 없다.
  - **명령이 갈 수 없으면 버튼이 잠겨야 한다.** live 의 `control`(bool)이 그 신호다 —
    보트 위 정본은 항상 참, 미러는 forwarder 가 붙어 있을 때만 참. 거짓이면 대시보드가
    버튼을 **비활성화**(숨기지 않는다)하고 "보트 연결 끊김 · 조작 불가"를 띄우며,
    서버도 `{"ok":false,"reason":"offline"}` 로 거절한다.
    조용히 삼키는 것이 최악이다 — 조작자는 윈치가 내려간 줄 알고 실제 윈치를 조작한다(6절).
  - **밀린 명령은 실행하지 않고 버린다.** forwarder 의 명령 큐는 16개 상한에 오래된 것부터
    버리고, Pi 소켓이 끊겨 있으면 그 자리에서 버린다. 몇 초 전 '내림'이 뒤늦게 실행되면
    조작자가 이미 손을 뗀 뒤라 실제 윈치와 어긋난다.
  - **ack 는 Pi 가 낸 것이 정본이다.** 미러가 주는 `{"ok":true,"relayed":true}` 는
    "전달됨"일 뿐이고, 진짜 판정은 Pi 의 ack 가 forwarder 를 타고 되돌아와 방송된다.

  남은 경계: 클라우드는 **수심 추정·status 판정을 재계산하지 않는다** (이건 그대로다).
  `live` 의 `mirror: true` 는 이제 읽기 전용 표시가 아니라 "여기는 클라우드 화면"이라는
  안내다 — 헤더에 "클라우드 · 원격 조작" 칩으로 뜨고, 끊기면 같은 자리가 경고색으로 바뀐다.
- 클라우드는 수심 추정·status 판정을 재계산하지 않는다. 라즈베리파이가 판정한 값을 그대로 저장·표시한다.

접속 경로: `https://<공인IP>.sslip.io` (Let's Encrypt 무료 인증서, 사용자 확정 2026-08-11).
WebSocket은 `wss://` 를 쓴다 — HTTPS 페이지에서 `ws://` 는 브라우저가 차단한다.
- ESP32 계층 삭제 (사용자 확정, 2026-08-10: 보트에서 직접 하강, DEC890 케이블 10m로 충분).
  1절 원시 스키마 {seq,ec,tds,temp}와 /ws/ingest 인터페이스는 유지 — 공급자만 raspi/feed.py로 교체.

역할 분담 (이 경계를 넘는 코드 금지):
- **raspi/feed.py (수집기)**: 1초 주기로 센서와 **부표 GPS** 를 읽어 최소 JSON을 /ws/ingest로 송신만. 가공·저장·수심 계산 금지.
  GPS 는 `raspi/gps.py` 가 별도 스레드로 NMEA 를 계속 읽고, feed.py 는 송신 시점의 최신 FIX 만 집어 간다
  (사용자 확정 2026-09-13 — GPS 하드웨어 장착). 좌표 보정·평활화 금지.
- **라즈베리파이 서버**: 수심 추정(윈치 시간 적분), GPS 병합, SQLite 저장, 브로드캐스트, CSV/XLSX 내보내기.
- **대시보드**: 표시 + 윈치 조작 명령(내림/정지/올림/auto 순차 측정) 송신만. 무거운 데이터 가공 금지.
  - **자동 순환(`auto`) 삭제, 측정 시작/완료 도입** (사용자 확정, 2026-08-28).
    명령은 `measure_start` | `down` | `stop` | `up` | `measure_end`
    | `survey_start` | `survey_end` 다.
    측정 시작 → 0.5 m 하강 후 **자동 정지**·30초 측정 → 30/30 초에서 "측정 완료" 표기 후 **대기**
    → `down` 을 눌러야 다음 층(1.0 → 1.5) → 측정 완료 → 0 m 부상.
    한 번의 측정 시작~완료 = **한 지점 1사이클**이고 측정 차수(survey)는 유지된다 —
    배를 옮겨 다시 측정 시작하면 같은 차수에 지점이 누적된다(3D 히트맵이 이렇게 채워진다).
    30초가 지난 뒤에는 측정 레코드를 더 만들지 않는다(대기 시간이 길어도 건수가 부풀지 않게).

## 1. 데이터 명세 (모든 코드는 이 스키마 기준)

raspi/feed.py → 서버 /ws/ingest (1 Hz):
```json
{"seq": 1024, "ec": 187.5, "tds": 93.0, "temp": 21.3,
 "lat": 36.575102, "lon": 127.221401, "gps_fix": true}
```
- pH 센서 제외, EC/TDS 일체형 센서로 확정 — `ph` 키를 `tds`로 교체 (사용자 확정, 2026-08-10).
  `tds`(ppm)는 센서가 계산해 주는 값을 그대로 전달(재계산 금지).
- **GPS 3키 추가** (사용자 확정, 2026-09-13 — 라즈베리파이에 GPS 모듈 장착).
  `lat`/`lon` 은 십진 도(소수 7자리), `gps_fix` 는 그 좌표가 **지금** 유효한가다.
  - **FIX 가 끊겨도 마지막 좌표를 지우지 않고 유지**하되 `gps_fix: false` 로 보낸다.
    좌표를 즉시 버리면 HOLD 30초 중 순간적인 끊김만으로 그 지점이 통째로 좌표를 잃어
    히트맵(X/Y 가 실제 위경도)에 못 올라간다. 대신 "지금 값이 아님"을 반드시 함께 전달한다(6절).
  - FIX 를 **한 번도** 못 잡았으면 `lat`/`lon` 은 `null` 이다 (JSON 에 NaN 금지).
  - **이 세 키의 유무가 "GPS 수집기인가"를 판정하는 유일한 신호다.** 키가 하나라도 오면
    서버는 그 뒤로 모의 좌표 생성을 영구히 멈춘다 — FIX 를 못 잡았을 뿐인데 가짜 보트가
    저수지를 돌아다니면 6절의 최악의 버그가 된다. 구버전 feed.py·목업은 키를 안 보내므로
    기존처럼 모의 좌표로 동작한다.

라즈베리파이 → 대시보드: **두 채널로 분리** (사용자 확정, 2026-08-05)

① `live` 메시지 (1 Hz 상시, **저장 안 함** — 게이지·수심·시계열·보트 위치 표시 전용):
```json
{"type": "live", "state": "DESCENDING", "depth_est": 0.73,
 "ec": 187.5, "tds": 93.0, "temp": 21.3,
 "lat": 36.5751, "lon": 127.2214, "status": "ok"}
```
- `lat`/`lon`: 부표(보트) GPS — 이동 중 지도 실시간 갱신용 (사용자 확정, 2026-08-05).
  실측 GPS 가 붙으면 그 값이 정본이고, 없을 때만 서버가 모의 좌표를 만든다.
- **GPS 표시 상태 `gps` 추가** (사용자 확정, 2026-09-13). 서버가 판정해 내려보내고
  대시보드는 그대로 표시만 한다 — 대시보드가 좌표 나이를 다시 추정하지 않는다.
  | 값 | 뜻 | 화면 |
  |---|---|---|
  | `ok` | 지금 잡힌 FIX | 초록 칩 "실측 GPS" |
  | `hold` | FIX 끊김 — **마지막 좌표 유지 중** | 주의색 배너 + 보트 마커 회색 |
  | `wait` | GPS 는 붙었으나 아직 FIX 없음 (좌표 null) | 주의색 배너 |
  | `none` | GPS 미연결 — 좌표는 모의값 | 주의색 칩 "모의 좌표" |
  `gps_age`(초)는 마지막 FIX 이후 경과 시간(없으면 null) — 경고 문구의 초 표시에 쓴다.
- **조작 가부 `control` 추가** (사용자 확정, 2026-09-13). 이 화면의 명령이 실제로
  라즈베리파이까지 갈 수 있는가. 보트 위 정본은 항상 `true`, 클라우드 미러는 forwarder 가
  붙어 있을 때만 `true`. 대시보드는 `false` 면 윈치·차수 버튼을 **비활성화**한다(숨기지 않는다 —
  버튼이 사라지면 조작자가 "원래 없는 화면"으로 오해한다). 키가 없는 구버전 서버에서는
  `true` 로 간주해 옛 동작을 유지한다.
  경고는 **주의색(`#B26A00`)** 이다. 수질값은 정상인데 위치만 못 믿는 상황이라,
  센서 이상(위험·빨강)과 같은 색이면 조작자가 구분하지 못한다.
  구버전 서버(키 없음)에 붙으면 대시보드는 GPS 경고를 띄우지 않고 옛 동작으로 폴백한다.
- **측정 진행 필드 추가** (사용자 확정, 2026-08-28): `measuring`(bool, 측정 시작~완료 사이),
  `hold_elapsed`(초, 이번 층의 측정 경과 — HOLD_SECONDS 에서 멈춘다), `hold_total`(초).
  이전에는 대시보드가 live 개수를 세어 추정했는데 재접속·틱 지연에 어긋나서 서버 정본으로 옮겼다.
  구버전 서버(필드 없음)에 붙으면 대시보드가 옛 방식으로 폴백한다.

② 측정 레코드 (**HOLD 구간에서만** 생성 — SQLite 저장·히트맵·내보내기 대상):
```json
{"ts": 1754373600, "survey": 3, "ec": 187.5, "tds": 93.0, "temp": 21.3,
 "depth": 1.0, "lat": 36.5751, "lon": 127.2214, "status": "ok"}
```
- `survey`: 측정 차수 id (`surveys.id`). **조작자가 "차수 시작"을 눌러야 생긴다** —
  전원만 켜져 있고 측정을 안 하면 차수도 레코드도 만들어지지 않는다 (사용자 확정 2026-08-29).
  차수가 열려 있지 않으면 서버가 `measure_start` 를 거절하고 대시보드는 버튼을 잠근다.
- `depth`: **항상 0.5 | 1.0 | 1.5 중 하나** — 이동(DESCENDING/ASCENDING) 중에는 측정 레코드를 생성하지 않는다
- `status`: `ok` | `stale`(2초 이상 미수신) | `fault`(범위 밖·NaN)
- NaN·미수신 값만 JSON `null`로 송신하고 DB에도 NULL로 저장한다 (JSON에 NaN 금지).
  범위 밖의 유효 수치는 **원값을 유지**하고 `status: "fault"`로 표시한다 — 센서 이상 진단 가치 보존, 집계 제외는 6절 규칙이 담당 (오케스트레이터 확정, 2026-08-05).
- stale 구간에는 측정 레코드를 생성하지 않는다 — 공백은 공백으로 표시 (6절).
- 윈치 보조정보(자동 여부·측정 경과 등)는 별도 채널 없이 대시보드가 live에서 파생 계산한다.
- 미장착 센서: 서버 설정 `UWD_SENSORS`(기본 `ec,tds,temp`)에 나열된 항목만 status 판정에 참여한다. 미장착 항목은 null 송신·NULL 저장 유지 (오케스트레이터 확정, 2026-08-05).
- 서버 목업 스트림은 **기본 비활성**(`UWD_MOCK=0`). 개발·시연 시에만 명시적으로 켠다 — 실기 feed 중단 시 목업이 조용히 이어받아 가짜 값을 ok로 표시하는 것 금지 (6절).
- 스키마에 키를 추가·개명할 때는 반드시 사용자 확인 후 이 절부터 수정.

### 조사 차수 = 조사지 + 날짜 + 회차 (사용자 확정, 2026-08-29)

`surveys` 테이블: `id, site, survey_date, round, memo, started_at, ended_at`.

- `round` 는 **같은 조사지·같은 날짜 안에서만** 1,2,3… 으로 올라간다.
  저수지를 옮기면 그곳의 1차부터 다시 시작한다 — **다른 저수지에서 4·5·6차가 되지 않는다.**
  날짜가 바뀌어도 1차부터다.
- `site`(조사지 이름)는 조작자가 대시보드 헤더에서 자유 입력한다. 최근에 쓴 이름은
  `GET /sites` 로 내려가 자동완성 목록이 된다.
- 명령: `survey_start`(+`site`, 선택 `memo`·`date`) / `survey_end`.
  종료하면 진행 중이던 측정은 버려진다(30초 미만은 기록하지 않는다).
- `live` 메시지에 `survey_open`·`survey`·`site`·`round`·`survey_date` 를 실어 보낸다 —
  대시보드가 즉시 버튼을 잠글 수 있어야 한다.
- 내보내기(CSV/XLSX)에는 `site, survey_date, round` 가 앞에 붙는다. 파일명도
  `<조사지>_<날짜>_<N>차` 다 — 파일만 봐도 어디 몇 차인지 알 수 있어야 한다.

### 값 범위·임계값 (세종 용암저수지 2010~2026 실측 76건 기반)

| 항목 | 정상 | 주의 | 위험 | 근거 |
|---|---|---|---|---|
| EC (µS/cm) | **≤280** (100 미만 포함) | 280~700 | >700 | 실측 116~256 / 위험선은 FAO 관개용수 기준 |
| TDS (ppm) | **≤140** (50 미만 포함) | 140~350 | >350 | EC×0.5 환산 근사 (센서 TDS 팩터 0.5 가정) |
| 수온 (℃) | 0~35 | — | 범위 밖 = fault | 실측 4.5~29.7 |

- **하한 미만은 정상이다** (사용자 확정, 2026-08-28). 예전 표는 EC<100 / TDS<50 을 '주의'로
  분류했으나 저농도는 수질 이상이 아니므로 정상(초록·가장 옅은 파랑)으로 바꿨다.
  적용 지점: `config.js` 의 `classify()`, 게이지 색 구간, 히트맵 램프.
- **게이지 눈금 범위**(표시용, 임계값 아님, 사용자 확정 2026-08-28):
  수온 0~50 ℃ / EC 0~1000 µS/cm / TDS 0~500 ppm — `config.js` 의 `GAUGE_SCALE`.

- pH 행 삭제 (센서 제외, 2026-08-10). TDS fault 판정선은 0~10000 ppm(물리적 불가능 값).

- **히트맵 정상 대역은 파랑 한 색**(`#2A78D6`)이다 (사용자 확정 2026-08-28 — 13단계
  파랑 그라데이션 폐기). EC 0~280 / TDS 0~140 전부 같은 파랑이고, 값의 크기는 **막대 높이**가
  이미 선형으로 나타낸다. 범례도 정상·주의·위험 세 칸의 색 표기로 통일한다.
  정상 상한 초과는 전용 경고색(주의 `#B26A00` / 위험 `#C62828`)으로 별도 표시.
- **히트맵 표현 형식 재변경**(사용자 확정, 2026-08-28): 3차원 산점 → **3차원 막대그래프**.
  X/Y 축은 레코드의 실제 **위도·경도** 격자이고 축 눈금에 좌표를 그대로 쓴다.
  막대는 **각 수심 층 바닥에서 값 크기만큼** 솟으며, 높이 스케일은 [0, 위험 임계값] 고정
  (EC 0~700 / TDS 0~350) — 배치마다 스케일이 바뀌지 않는다.
  막대·지도 마커를 누르면 **같은 측정 지점끼리 하이라이트가 동기화**된다
  (집계는 `dashboard/src/stations.js` 한 곳에서만 — 두 패널이 같은 station id 를 본다).
  롤백 지점: 산점 구현은 커밋 `0f5c738`.
- **히트맵 표현 형식 변경**(사용자 확정, 2026-08-26): 깊이별 평면 슬라이스가 아니라
  **(X, Y, 수심) 3차원 산점**으로 세 수심층을 한 화면에 쌓아 보여준다. 정상 도메인 안은
  **파랑 단일 순차 램프 13단계 + 사각형**, 도메인 초과는 **다이아몬드**(주의 `#B26A00` /
  위험 `#C62828`) — 색만으로 구분하지 않고 형태까지 바꾼다. 팔레트 정의는
  `dashboard/src/config.js` 의 `HEATMAP_3D`. **바뀐 것은 그리는 방식뿐이고 임계값과
  컬러 도메인은 이 절의 표 그대로다.**
- **TDS 3D 히트맵 추가**(사용자 확정, 2026-08-16) — EC 히트맵과 표시 항목 탭으로 전환. 정상
  상한은 **140**(위 표와 동일), 350 초과는 EC와 같은 경고색 규칙.
  새 임계값을 만든 것이 아니라 이 표의 기존 TDS 정상/위험 값을 히트맵에 재사용한 것이다.
- 주의/위험 셀 강조는 **색상과 테두리로만**. 막대 높이(elevation)는 항상 실측값 비례 — 값을 왜곡하는 확대 금지.

### 수심 추정 상수 (실측 보정 전까지 가안)

```
DESCENT_RATE = 0.5/30  # ≈0.01667 m/s (30초 → 0.5 m, 사용자 확정 2026-08-10)
ASCENT_RATE  = TBD     # 상승 속도 실측 후 기입
DEPTH_LEVELS = [0.5, 1.0, 1.5]   # 화면 표기: 표층 0.5 m / 중층 1.0 m / 저층 1.5 m
```
- 윈치 구동: **ESP32가 모터를 시간 기반으로 별도 제어** (30초=0.5m, 기존 아두이노 코드).
  서버 상태기계는 같은 상수로 병행 적분 — 조작자가 실제 윈치 시작과 동시에 대시보드 명령 입력.
  ESP32↔서버 자동 동기화(시작 신호 전달)는 선택 개선 항목.
  대시보드 수심 카드에는 이 규칙을 한 줄로 적는다 — "내림·정지·올림은 실제 조작과 동시에"
  (사용자 요청 2026-08-28: 두 줄로 접히지 않게 짧게. 전체 문장은 `title` 툴팁에 둔다).
- 상태기계: SURFACE → DESCENDING → HOLD(측정) → ASCENDING → SURFACE. 라즈베리파이가 소유하고, 대시보드는 명령만 보낸다.
  자동 진행은 없다(2026-08-28) — 층 이동은 항상 조작자의 명령으로만 일어난다.
- **수심 층 이름**(사용자 확정 2026-09-02): 0.5 m = 표층, 1.0 m = 중층, 1.5 m = 저층.
  화면에는 이름과 수치를 함께 쓴다("표층 0.5 m"). 정의는 `config.js` 의 `DEPTH_LAYERS` 한 곳뿐이고
  히트맵 평면·레일 탭·툴팁·하단바가 모두 여기서 가져다 쓴다.
- 수압센서(MS5837) 도입 시 이 절 전체를 실측 수심으로 교체.

## 2. Think Before Coding (코딩 전 확인)

- 스키마·임계값·상수는 **1절이 정답**. 문서에 없는 값만 질문하세요.
- 반드시 질문 후 진행: 핀 배치 변경, 통신 규격 변경, 임계값·보정 계수 변경.
- 알아서 판단해도 됨: UI 색상, 변수명, 차트 스타일 등 비파괴적 선택.
- 모호하면 코드를 짜지 말고 질문하고, 복잡한 설계 대신 가장 단순한 구조를 먼저 제안하세요.

## 3. Simplicity & Real-time First (실시간성과 단순성)

- 프론트엔드: **Vite + React** (사용자 확정, 2026-08-16 — 기존 "순수 JS, React 금지" 결정을 뒤집음).
  가독성·컴포넌트 재사용을 위한 전환이며, 상태관리 라이브러리(Redux 등)는 도입하지 않는다
  (React Context + useReducer로 충분 — 이 절 "단순성" 원칙은 유지).
  기존 `ws-client.js`의 재연결 로직과 `config.js`의 상수는 그대로 옮기고 로직을 새로 쓰지 않는다(4절).
- 지도: **MapLibre GL + VWorld 위성 타일**, 그 위에 deck.gl 오버레이(현재 위치·이동 궤적).
- 3D 히트맵: **SVG 3D 막대그래프**(`components/Heatmap3D.jsx`, 2026-08-28). 세 수심층을 한 화면에 쌓아 그리고,
  좌측 레일의 깊이 탭(전체 / 0.5 / 1.0 / 1.5 m)은 강조 대상을 고르는 역할이다.
  deck.gl GridLayer 는 이 형식으로 대체됐다 (사용자 확정, 2026-08-26).
- **표시 항목(EC/TDS)·깊이 선택은 좌측 레일이 단독 소유**한다 (사용자 확정, 2026-08-26).
  히트맵 패널 안에 같은 탭을 두지 않는다 — 한 선택을 두 곳에서 하면 어긋난다.
- 라이트 테마: 캔버스 `#F2F2F2` · 패널 흰 카드 · 헤더 바만 `#96D0FF` · 파란 배경 위 글자는
  `#0F2B3D`(흰 글씨 금지) · 노랑 `#FFD400` = 지금 장비가 있는 위치(수심 프로브·보트 마커).
  명세 정본은 `design/UI_REQUIREMENTS.md`.
- 갱신 주기 규칙: 게이지 **1초** / 히트맵·지도 궤적 **3초 배치** / UI 전체 리렌더 금지.
- **시계열 차트(Chart.js) 삭제**(사용자 확정, 2026-08-26) — 새 4열 레이아웃에 자리가 없다.
  `panels/charts.js` 와 `ChartPanel.jsx` 는 제거됐다.
- SQLite 쓰기는 5초 단위 트랜잭션 배치로 SD카드 마모 최소화.
- 스스로 질문: "이 코드가 ESP32·라즈베리파이 CPU에 무리를 주지 않는가?"

## 4. Surgical Changes (정밀 수정)

- 핀 맵 (배선 확정 후 GPIO 번호 기입 — 확정 전 임의 지정 금지, 확정 후 무단 변경 금지):
```
EC/TDS 일체형 센서 → 라즈베리파이 USB-RS485 (/dev/ttyUSB0)
           Modbus-RTU 주소 5, 9600-8N1, timeout 1.0s
           읽기: FC03 홀딩 레지스터, uint16 정수
                 reg0 = 수온 (원시값 ×0.1 ℃) / reg1 = EC (µS/cm) / reg4 = TDS (ppm)
           검증된 관행: 트랜잭션 전 버퍼 클리어, 호출 후 포트 닫기, 레지스터 간 50ms 간격
           ※ DEC890 매뉴얼(주소 17/FC04/float byte-swap)과 불일치 — 실장착 센서의
             동작 확인된 소스코드 기준 (사용자 제공, 2026-08-10). DEC890 자료는 참고용.
pH       → 제외 (사용자 확정, 2026-08-10)
GPS      → 라즈베리파이 GPIO UART (/dev/ttyAMA0), 9600-8N1, NMEA 0183
           (사용자 확정 2026-09-13 — 부표 GPS 모듈 장착)
           파싱: GGA(위치+위성수) · RMC(위치 백업) 둘 다. 모듈·펌웨어에 따라 한쪽만
                 꾸준히 나오는 경우가 있어 하나만 보면 "RAW 는 오는데 FIX 가 안 잡힘" 이 된다.
           검증: `*HH` XOR 체크섬 필수. 보트 배선은 잡음을 타서, 깨진 한 글자가
                 좌표를 저수지 밖으로 날려 보내고 그 값이 측정 레코드에 그대로 박힌다.
           거부: 분(minute) ≥ 60 · 위경도 범위 밖 · (0,0) — 전부 미초기화/깨진 문장이다.
           FIX 유지 한계 10초 (`--gps-fix-timeout`) — 넘으면 gps_fix=false, 좌표는 유지.
           ※ 포트가 콘솔로 잡혀 있으면 `raspi-config` 에서 serial 콘솔을 끄고 하드웨어
             UART 만 켠다. 기종에 따라 /dev/serial0 이 별칭이다.
윈치     → ESP32 별도 제어 (1절 수심 상수 참조, 대시보드/서버와 무배선)
```
### 레지스터 맵 실측 검증 (2026-08-13, `tools/dec890_air_tapwater_*.csv` 3건)

주소 5 / 9600 / FC03 으로 레지스터 0~20을 읽어 공기 → 액체 변화를 비교한 결과, 위 맵이 확인됨.

| 시료 | reg0 ×0.1 | reg1 (EC) | reg4 (TDS) | TDS/EC |
|---|---|---|---|---|
| 수돗물 | 21.0 ℃ | 234 | 116 | 0.496 |
| 비눗물 | 25.3 ℃ | 1464 | 732 | 0.500 |
| 미지근한 물 | 30.3 ℃ | 292 | 145 | 0.497 |

- **TDS/EC = 0.5 가 세 시료 모두에서 성립** → `reg1=EC`, `reg4=TDS`, 1절의 `TDS_FACTOR=0.5` 가정이 모두 실측으로 뒷받침됨.
- 비눗물이 수돗물의 6배로 읽힌 것은 정상 반응(이온성 계면활성제). 센서 이상 아님.
- **`reg2` = 염도(ppt ×0.01)** 로 추정 — 세 시료 모두 `reg1/reg2 ≈ 20.9`. 현재 미사용이나 필요 시 사용 가능.

### ⚠️ 미해결: 온도 보정 여부 (실측 필요)

같은 계열 시료에서 수온 21.0 ℃ → 30.3 ℃ 일 때 EC 234 → 292 (+25%). 전도도의 온도계수
(약 2%/℃)로 예측한 +18.6% 와 대체로 일치 → **센서가 25 ℃ 환산 없이 날값을 줄 가능성이 높다.**

이것이 문제가 되는 이유: 1절의 정상 범위 100~280 µS/cm 는 수질 성적서 기준(25 ℃ 환산)에서 왔는데,
용암저수지 수온은 4.5~29.7 ℃ 로 변한다. 보정이 없다면 겨울에는 약 35% 낮게, 여름에는 약 10% 높게
읽혀 **히트맵이 수질이 아니라 수온을 반영하게 된다.**

판정 절차 (어댑터 연결 후 표준액 검증과 함께 수행):

1. 1413 µS/cm 표준액을 **25 ℃** 에서 측정 → 1342~1484 이면 센서 정확도 합격(±5%)
2. **같은 용액을 30 ℃ 로 데워 재측정** → 값이 오르면 보정 없음, 그대로면 보정 있음

2번 결과가 "보정 없음"이면 **코드를 고치기 전에 사용자에게 선택지를 제시한다** (아래 금지 조항 유지).
표준액이 없으면 임의 소금물로도 경향 판정은 가능하다(정확도 판정은 불가).

- **레지스터는 하나씩 읽는다 — 블록 읽기로 바꾸지 않는다** (사용자 확정 2026-09-13 재확인).
  `hi_flow_collector.py` 는 `read_registers(0, 5, FC03)` 로 5개를 한 번에 읽고 포트를 열어 두었으나,
  실장착 센서에서 동작이 확인된 관행은 위의 "개별 읽기 + 버퍼 클리어 + 호출 후 포트 닫기" 다.
  검증된 쪽(`raspi/ects.py`)을 유지하고, 블록 읽기로 바꾸려면 현장에서 실패율을 비교한 뒤 이 절을 먼저 고친다.
- 보정: 이 센서는 교정 레지스터 정보가 없음 — 1413 µS/cm 표준액 **대조 검증**(±5%)으로 갈음.
  라즈베리파이 코드에서 EC/TDS 값 재보정 금지. 편차가 5%를 넘으면 센서 벤더 문의 후 이 절에 기입.
- 통신 규격(WebSocket 경로, JSON 키)은 1절 스키마 외 임의 추가 금지.
- 대시보드 수정 시 웹소켓 재연결 로직(지수 백오프, 최대 10초) 절대 보존.

## 5. Goal-Driven Execution (단계별 검증)

각 단계 완료 검증 전에는 다음 단계 코드를 작성하지 않는다.

| 단계 | 산출물 | 완료 기준 |
|---|---|---|
| 1 | 목업 데이터 생성기(실측 범위 반영) + 대시보드 전 패널 | 가짜 1 Hz 스트림으로 게이지·지도·히트맵·차수 선택 모두 동작 |
| 2 | FastAPI WebSocket/REST + SQLite | 목업 → 저장 → 차수별 조회 → CSV 내보내기 왕복 성공 |
| 3 | 실기 연동(센서+GPS) + 센서 교정 | 표준액 기준 오차 ±5% 이내, 1시간 연속 송신 무손실, 저수지에서 GPS FIX 획득·궤적 일치 |
| 4 | 현장(저수지) 시험 | Wi-Fi 끊김 복구, stale 표시, 1회 조사 전체 기록 무손실 |

대시보드 필수 패널: 좌측 레일(표시 항목·깊이 슬라이스·내보내기) / 3D 히트맵(산점) / 현재 수심(하강 진행 표시) /
수온·EC·TDS 게이지 / 위성 지도(현재 위치·궤적) / 측정 차수 선택.

## 6. 안전·실패 모드 (가장 중요)

- **오래된 데이터를 정상처럼 보여주는 것이 최악의 버그다.** 2초 이상 미수신 시 마지막 값을 회색 처리하고 "연결 끊김"을 명시 표시.
- NaN·범위 밖 값은 버리지 말고 `fault` 플래그로 **저장은 하되**, 히트맵 집계에서는 제외.
- Wi-Fi 재접속 시 ESP32는 미전송분 재전송 없이 현재값부터 송신(단순성 우선). 공백 구간은 대시보드에 공백으로 표시.
- 조사 종료 시 SQLite 파일과 차수별 CSV를 함께 백업(USB 복사 또는 `/export` 엔드포인트).

## 7. 모델 역할 분담 (오케스트레이션 — 2026-08-10 개편)

| 역할 | 모델 | 담당 |
|---|---|---|
| 기획·구현 | Opus 5 | 요구 분석, 설계·작업 계획 수립, 코드 작성 (5절 단계 범위 내) |
| 리뷰·재검토 | Sonnet 5 | 구현 결과를 이 문서 기준으로 검토 + 수정 반영분 재검토 |

- 스키마·임계값·상수·아키텍처 변경의 **최종 확정은 사용자** — Opus 5는 계획 단계에서 이런 변경이 필요하면 코드 작성 전에 질문한다 (2절).
- 구현 결과는 **Sonnet 5 리뷰 통과 전에는 단계 완료로 처리하지 않는다.** 지적 사항 반영 후 재검토(델타 리뷰)까지 받는다.
- 리뷰 기준: ① 1절 스키마 준수 ② 3절 갱신 주기·스택 준수 ③ 6절 실패 모드 구현 여부 ④ 불필요한 추상화·프레임워크 유입 여부.
- 확정된 결정은 이 문서에 날짜와 함께 기록한다 (결정 이력은 PROJECT_STATUS.md 참조).
