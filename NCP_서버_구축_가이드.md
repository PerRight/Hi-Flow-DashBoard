# 네이버 클라우드(NCP) 서버 구축 가이드

> 수중드론 대시보드 서버를 NCP VPC 환경에 올리기 위한 절차서.
> 작성: 2026-08-11 / 대상: KR-1 Zone, Ubuntu 24.04, Micro Server(mi1-g3)
> ⚠️ 아키텍처 확정 전 임시 문서 — 서버 구성 방식(하이브리드/전체이전)은 CLAUDE.md에 미반영 상태.

---

## 0단계. 기존 리소스 정리 (삭제는 반드시 역순)

의존성 때문에 아래 순서를 지켜야 합니다. 순서를 틀리면 "사용 중인 리소스입니다" 오류가 납니다.

| 순서 | 대상 | 메뉴 위치 | 동작 |
|---|---|---|---|
| 1 | **서버** | Server > Server | 정지 → **반납** |
| 2 | **공인 IP** | Server > Public IP | 서버에서 **할당 해제** → **반납** |
| 3 | **서브넷** | VPC > Subnet Management | 삭제 |
| 4 | **VPC** | VPC > VPC Management | 삭제 |

체크 포인트

- 서버는 **정지 후에만** 반납됩니다. 반납 보호가 걸려 있으면 먼저 해제.
- 공인 IP는 서버를 지워도 **자동으로 반납되지 않습니다.** 남아 있으면 월 4,032원이 계속 나갑니다. 반드시 확인.
- 서브넷이 하나라도 남아 있으면 VPC는 삭제되지 않습니다.
- ACG(방화벽)는 VPC에 딸려 함께 삭제됩니다. 따로 지울 필요 없음.

---

## 1단계. VPC 생성

**VPC > VPC Management > `+ VPC 생성`**

| 항목 | 값 | 비고 |
|---|---|---|
| VPC 이름 | `hiflowserver` | |
| IP 주소 범위 | **`10.0.0.0/16`** | ⚠️ 서브넷 값(`10.0.1.0/24`)을 여기 넣지 말 것 |
| 유형 | `NORMAL` | TRANSIT은 VPC 여러 개를 묶을 때만 |

**핵심: VPC의 CIDR은 생성 후 변경할 수 없습니다.** `/16`으로 넉넉히 잡아야 나중에 로드밸런서·DB용 서브넷을 추가할 수 있습니다.

`/24`로 만들면 서브넷 하나가 VPC 전체를 차지해서, 나중에 LB를 붙일 때 (NCP는 LB 전용 서브넷을 별도로 요구) 다시 만들어야 합니다.

상태가 **운영중**으로 바뀔 때까지 대기 (약 1분).

---

## 2단계. Subnet 생성

**VPC > Subnet Management > `+ Subnet 생성`**

| 항목 | 값 | 비고 |
|---|---|---|
| Subnet 이름 | `hiflow-public` | |
| VPC | `hiflowserver (10.0.0.0/16)` | |
| IP 주소 범위 | **`10.0.1.0/24`** | VPC 범위 **이하**여야 함 |
| 가용 Zone | **`KR-1`** | 서버·공인 IP와 **반드시 동일** |
| Network ACL | `hiflowserver-default-network-acl` | 기본값 |
| Internet Gateway 전용 여부 | **`Y (Public)`** | 공인 IP를 붙이려면 필수 |
| 용도 | **`일반`** | LoadBalancer/BareMetal/NatGateway 아님 |

**Zone은 KR-1로 통일하세요.** 서버, 서브넷, 공인 IP가 서로 다른 Zone에 있으면 연결되지 않습니다. 서버 생성 실패의 흔한 원인입니다.

---

## 3단계. ACG(방화벽) 인바운드 규칙

**Server > ACG > `hiflowserver-default-acg` 선택 > ACG 설정**

기본 ACG는 인바운드가 전부 막혀 있어 SSH조차 안 됩니다. 아래를 추가하세요.

| 프로토콜 | 접근 소스 | 허용 포트 | 용도 |
|---|---|---|---|
| TCP | `내 공인IP/32` | `22` | SSH 접속 |
| TCP | `내 공인IP/32` | `8000` | FastAPI 개발 확인용 (임시) |
| TCP | `0.0.0.0/0` | `80` | 나중에 nginx/HTTPS 발급용 |
| TCP | `0.0.0.0/0` | `443` | 대시보드 공개 접속 |

- 내 공인 IP는 검색창에 "내 아이피"를 치면 확인됩니다. 유동 IP라 바뀌면 규칙도 갱신해야 합니다.
- **22번을 `0.0.0.0/0`으로 열지 마세요.** 공개된 SSH 포트는 몇 시간 안에 무차별 대입 공격을 받습니다.
- 8000번은 라즈베리파이가 붙을 주소이기도 합니다. 라즈베리파이가 LTE 유동 IP라면 이 포트는 결국 443(HTTPS)으로 통합하는 게 안전합니다.

---

## 4단계. 서버 생성 (6단계 마법사)

**Server > Server > `+ 서버 생성`**

### 4-1. 서버 이미지 선택

- `Ubuntu 24.04` (라즈베리파이와 환경을 맞추기 위함)

### 4-2. 서버 설정

| 항목 | 값 | 주의 |
|---|---|---|
| VPC | `hiflowserver` | |
| Subnet | `hiflow-public \| KR-1 \| 10.0.1.0/24 \| Public` | Public이어야 공인 IP 연결 가능 |
| 서버 스펙 | **`Micro`** → `mi1-g3` | 결제수단 등록 후 1년 무료 / 계정당 1대 |
| 요금제 | 상시 가동이면 `월 요금제`, 껐다 켰다 하면 `시간 요금제` | |
| 서버 개수 | `1` | 공인 IP 동시 생성 조건 |
| 서버 이름 | `hiflow-server` | hostname 체크박스 유지 |
| **Network Interface** | eth0 행에서 **`+ 추가` 버튼 클릭** | ⚠️ 안 누르면 "디바이스를 추가해 주세요" 오류 |
| 공인 IP | **`새로운 공인 IP 할당`** | 미설정이면 외부 접속 불가 (월 4,032원) |
| 물리 배치 그룹 | `미사용` | |
| 반납 보호 | `설정` | 실수 삭제 방지 |

### 4-3. 스토리지 설정

- 크기 `10` GB, 볼륨 타입 `CB1` — **그대로 두기**
- micro 서버는 CB 볼륨 10GB까지 무료. 11GB부터 과금.
- `+ 스토리지 추가` 누르지 말 것 (생성 시점부터 별도 과금)

### 4-4. 인증키 설정 🔑

- `새로운 인증키 생성` → 이름 입력 → **`.pem` 파일 다운로드**
- **재발급 불가.** 잃어버리면 서버를 새로 만들어야 합니다.
- 프로젝트 폴더(`수중드론 대시보드/`) 안에 두지 말 것 — git에 올라갑니다.
- 권장 보관 위치: `C:\Users\skc59\.ssh\` 또는 별도 안전한 폴더

### 4-5. 네트워크 접근 설정

- 3단계에서 규칙을 넣은 ACG 선택

### 4-6. 최종 확인

- Zone이 전부 `KR-1`인지, Subnet이 `Public`인지 마지막으로 확인 후 생성
- 생성 완료까지 5~10분

---

## 5단계. 서버 접속

### 5-1. 관리자 비밀번호 확인

**Server > Server > 서버 선택 > `서버 관리 및 설정 변경` > `관리자 비밀번호 확인`**

→ 4-4에서 받은 `.pem` 파일 내용을 붙여넣으면 root 비밀번호가 복호화되어 표시됩니다.

### 5-2. SSH 접속

```bash
ssh root@<공인IP>
```

Windows에서는 PowerShell 또는 PuTTY 사용.

### 5-3. 접속 후 최소 설정

```bash
# 패키지 갱신
apt update && apt upgrade -y

# Python·git 설치
apt install -y python3-pip python3-venv git nginx

# 작업 사용자 생성 (root 상시 사용 지양)
adduser hiflow
usermod -aG sudo hiflow
```

---

## 서버 생성 실패 시 확인 목록

| 증상 | 원인 | 조치 |
|---|---|---|
| "디바이스를 추가해 주세요" | Network Interface 미추가 | 4-2에서 `+ 추가` 클릭 |
| 공인 IP 옵션 비활성 | Subnet이 Private이거나 서버 개수 ≠ 1 | Public Subnet, 개수 1로 |
| Zone 불일치 오류 | 서브넷/공인 IP Zone 다름 | 전부 KR-1로 통일 |
| micro 서버 생성 불가 | 계정당 1대 제한, 이미 보유 | 기존 micro 반납 후 재시도 |
| 결제 관련 오류 | 결제수단 미등록 | 마이페이지에서 카드 등록 |
| 서브넷 IP 부족 | 서브넷 대역이 너무 작음 | `/24` 이상으로 재생성 |

---

## 다음 작업 (서버 구축 후)

1. **아키텍처 확정** — 하이브리드(라즈베리파이 SQLite 정본 + 클라우드 미러) vs 전체 이전
   - CLAUDE.md §6 "1회 조사 전체 기록 무손실" 기준상 하이브리드 권장
   - 확정 후 CLAUDE.md §0 시스템 구성에 날짜와 함께 기록
2. 서버에 `server/` 배포 + systemd 유닛 등록
3. nginx 리버스 프록시 + Let's Encrypt HTTPS (WebSocket `wss://` 필수)
4. 라즈베리파이 → 클라우드 forwarder 작성 (끊김 시 로컬 큐잉·재접속 백필)
5. `dashboard/` 빌드 산출물(`dist/`) 배포
